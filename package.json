{
  "name": "send-that-invoice-cdk",
  "version": "2.0.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "cd src && jest",
    "cdk": "cdk",
    "pre-build": "cd src && npm install",
    "local": "cd src && npm run start",
    "copy-data": "auto/copy-data-folder-to-s3.sh"
  },
  "devDependencies": {
    "aws-cdk-lib": "^2.0.0",
    "constructs": "^10.2.66",
    "ts-node": "^8.4.1",
    "typescript": "~3.9.0"
  },
  "dependencies": {
    "@types/node": "^20.3.3",
    "aws-cdk-lib": "^2.0.0",
    "constructs": "^10.2.66"
  }
}
